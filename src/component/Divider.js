import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { global } from "../styles/global";

const Divider = () => {
  return <View style={global.divider}></View>;
};

export default Divider;
